# Teleport Lab — Configuration direnv
# Charger avec : direnv allow
# Les valeurs ci-dessous sont les défauts ; modifie-les selon tes besoins.

# Cluster Teleport
export TELEPORT_VERSION="17"   # Version majeure du dépôt APT Teleport

# Noms des VMs Multipass
export BASTION_VM="teleport-bastion"
export TARGET_VM="target-server"

# Ressources VMs
export VM_CPUS="2"
export VM_MEM="4G"
export VM_DISK="20G"
export UBUNTU_IMAGE="24.04"

# Charger les outils définis dans .mise.toml
mise trust -qa
mise install
